package com.project.roku.medical_entities;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.ForeignKey;
import jakarta.persistence.Table;
import org.springframework.data.annotation.Id;


/*
@Entity
@Table(name="medication_history")
public class MedicationHistory {

    @Id // primary key
    @Column(name="medication_history_id")
    private long medicationHistoryId;

    // @ForeignKey
    @Column(name="patient_id") // okay I'm using a foreing key here. Things might be different now.
    private int patientId;
}




 */